# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- **U09 (revised) — Group Flow.** Spec revised and re-implemented to add participation mode selection (solo vs group), group search, searchable MemberSelect, PIC-only member management in edit, and unified admin table showing all participants. Migration 0017 pending.

## Current Goal

- Run migration **0017_participation_mode** against live Supabase.
- Run migrations **0012–0016** if not yet applied (exercises, quizzes, quiz answer cascade).
- Manual test U09 revised: undecided CTA → "Go solo" sets mode; "Create group" → group created; search finds group by name + member name; PIC can add/remove members in edit; non-PIC cannot; Switch to Solo from group view works; admin `/admin/groups` shows Members/Size/Group name/Project idea columns with solo rows having empty Group name.
- Manual test U14: visit `/admin/users`, assign track, promote to admin, clear history with email challenge.
- Manual test U13: create a quiz in admin, take it as a learner, verify scoring, retake, unlock gate.

## Completed

- `project-overview.md` — V1 scope, user flow, in/out of scope, success criteria.
- `architecture.md` — stack, folder boundaries, storage model, auth, 10 invariants.
- `code-standards.md` — General, TypeScript, Next.js, Styling, API Routes, Data, File Organization.
- `ai-workflow-rules.md` — incremental spec-driven workflow, scoping, sync rules, exit criteria per unit.
- `ui-context.md` — colors, typography, radius, spacing, layout patterns, icons.
- **01 — Design System.** shadcn/ui (base-nova style, @base-ui/react) initialized; 19 components in `components/ui/`; `lucide-react` installed; `lib/utils.ts` `cn()` helper; globals.css with full design token `:root` block mapped to shadcn semantics; layout.tsx with Inter + JetBrains Mono via `next/font`; `TooltipProvider` at root; `typecheck` script added (`tsc --noEmit`); `page.tsx` renders `<Button>` + `<Card>` using only tokens; `npm run typecheck` + `npm run build` green. **DoD met.**
- **02 — Foundations.** `page.tsx` renders "Hello — AI Training Platform"; `next.config.ts` has `reactStrictMode: true` + `typedRoutes: true` (stable, not experimental in Next.js 16); Prettier + `prettier-plugin-tailwindcss` installed; `.prettierrc` + `.prettierignore` committed; `format` script added; Husky pre-commit hook running `lint-staged` (Prettier + ESLint on staged files); `.github/workflows/ci.yml` (Node 20, `npm ci` → typecheck → lint → build on PRs); `.env.local.example` committed with Supabase URL/anon/service-role placeholders; `.gitignore` updated to allow `.env.local.example`; `docs/decisions.md` seeded with ADR-001 (stack) and ADR-002 (design tokens); placeholder folders created for `lib/db`, `lib/auth`, `lib/schemas`, `lib/domain`, `lib/supabase`, `db/migrations`, `components/learner`, `components/admin`, `components/shared`, `app/(learner)`, `app/(admin)`; `npm run typecheck` + `npm run lint` + `npm run build` all green. **Note:** `eslint-plugin-tailwindcss` removed from ESLint — its worker thread cannot resolve the Tailwind v4 CSS-only package; class ordering is covered by `prettier-plugin-tailwindcss` at format time. **DoD met (pending Vercel deploy + CI branch protection).**
- **03 — Auth.** `@supabase/ssr` + `@supabase/supabase-js` installed; `lib/supabase/{client,server,middleware}.ts` factories; `lib/auth/admin-emails.ts` (ngan@pelago.co + 2 TODOs); `lib/schemas/user.ts` (Zod); `lib/db/users.ts` (`upsertUserOnSignIn`, `getUserById` via service role); `lib/auth/session.ts` (`getSessionUser`, `requireLearner`, `requireAdmin`); `app/auth/callback/route.ts` (domain check → sign out on failure → upsert → set `app_metadata.role` via admin API → redirect `/dashboard`); `app/sign-in/page.tsx` + `sign-in-button.tsx` (client, `signInWithOAuth`); `app/sign-out/route.ts` (POST → signOut → redirect `/sign-in`); `components/shared/sign-out-button.tsx`; `proxy.ts` at root (Next.js 16 renamed `middleware.ts` to `proxy.ts`; guards `/admin/*` using JWT `app_metadata.role`); `db/migrations/0001_users.sql` (users table + RLS); `db/seed.ts`; `docs/decisions.md` ADR-003; `app/page.tsx` redirects → `/dashboard` or `/sign-in`; `app/(learner)/dashboard/page.tsx` + `app/(admin)/admin/page.tsx` placeholders. **Note:** Two admin emails (Data Lead, Boss) left as TODO in `lib/auth/admin-emails.ts` — fill in before go-live. **DoD pending manual test with live Supabase Google OAuth.**
- **04 — Data Layer.** `vitest` + `tsx` installed; `npm run test` + `npm run seed` scripts added. Migrations: `0002_modules.sql`, `0003_materials.sql`, `0004_material_clicks.sql`, `0005_groups.sql`, `0006_group_members.sql` — each with RLS policies matching the architecture matrix. Partial unique index `one_pic_per_group` enforces single PIC per group. Zod schemas: `lib/schemas/{module,material,click,group,groupMember}.ts`. DB access layer: `lib/db/{modules,materials,clicks,groups,groupMembers}.ts` — session client for learner reads, service client for admin writes. Domain helpers: `lib/domain/{cutoffs,unlock,completion,dashboardState}.ts` — pure functions, no I/O. Unit tests: `lib/domain/__tests__/{unlock,completion}.test.ts` (15 tests, all green). `db/seed.ts` extended: 8 modules × 3 materials (24 total), covers all 5 material types, mix of compulsory/optional, weeks 1–2 `non_tech_only`. `docs/decisions.md` ADR-004 (partial unique index for PIC) + ADR-005 (insert-only clicks). `npm run typecheck` + `npm run test` + `npm run build` green. **DoD met (pending `npm run seed` against live Supabase).**
- **05 — Learner Dashboard.** `react-hook-form` + `@hookform/resolvers` installed. `app/onboarding/page.tsx` + `actions.ts` + `_components/OnboardingForm.tsx` — RHF+Zod form writes `name` + `track`, redirects to `/dashboard`. `app/(learner)/layout.tsx` — server component sidebar shell; redirects to `/onboarding` if `track` is null. Components: `SidebarShell` (client, mobile menu state), `SidebarNav` (client, `usePathname` active highlight, admin link gated by role), `ProfileMenu` (client, base-ui DropdownMenu, programmatic POST sign-out), `PageHeader` (client, mobile toggle). Learner components: `ProgressRing` (pure SVG, `strokeDasharray`), `TimelineStrip` + `ModuleNode` (done/current/locked states, lock tooltip), `ContinueCard`, `GroupCard` (placeholder). `app/(learner)/dashboard/page.tsx` fetches modules + materials + clicks in parallel, calls `dashboardState()`, renders all components. Stub pages created for `/modules`, `/modules/[id]`, `/group`. **Note:** base-ui components use `render` prop (not Radix `asChild`) — all composable wrappers use that pattern. typedRoutes types updated manually; will auto-regenerate on next build. `npm run typecheck` + `npm run build` green. **DoD pending manual test with seeded users.**
- **06 — Module Page.** `app/(learner)/modules/[id]/page.tsx` — server component; fetches module + all materials + clicks in parallel; computes locked state via `isModuleUnlocked()`; 404s for unknown modules and tech-track learners accessing `non_tech_only` modules. `app/api/track-click/route.ts` — POST handler; validates `materialId` from form data with Zod; checks session (401 on no session), validates UUID (400), checks material/module exist (404), enforces track filter (404), calls `recordClick()`, `revalidatePath` on dashboard + module, 302 to external URL. Components: `MaterialTypeIcon` (Lucide icon map for 5 types), `MaterialRow` (`'use client'`, form POST to `/api/track-click` with `target="_blank"`, `router.refresh()` on submit; locked rows wrapped in base-ui Tooltip), `MaterialList` (server component, passes `isCompleted` booleans to MaterialRow), `LockedModuleBanner` (lock icon + message with prev module title). Per-module compulsory progress bar via shadcn `Progress`. `npm run typecheck` + `npm run build` green. **DoD pending manual test with seeded users.**
- **08 — Admin Module & Material CRUD.** `app/(admin)/admin/modules/page.tsx` — module list table with week/order/track/material counts, "New module" button. `app/(admin)/admin/modules/[id]/materials/page.tsx` — material list for a module with back-link. Server actions (both `actions.ts` files, `"use server"`): `createModule`, `updateModule`, `deleteModule`, `reorderModules`; `createMaterial`, `updateMaterial`, `deleteMaterial`, `reorderMaterials` — each calls `requireAdmin()` and revalidates affected paths. DB: `lib/db/modules.ts` + `lib/db/materials.ts` extended with `adminDeleteModule` (HAS_CLICKS guard via material_clicks count check; service client bypasses missing DELETE RLS policy), `adminReorderModules` (parallel SELECT + two UPDATEs; no unique constraint on order), `adminDeleteMaterial`, `adminReorderMaterials`. Components: `ModuleListTable` + `MaterialListTable` (client; manage formDialog + deleteDialog state; invoke reorder via `useTransition`), `ModuleRow` + `MaterialAdminRow` (presentational, include ReorderButtons), `ModuleForm` + `MaterialForm` (RHF+Zod; create/edit modes; toast on success/error), `ConfirmDeleteDialog` (controlled base-ui Dialog), `ReorderButtons` (up/down icon buttons). Toaster added to root layout. SidebarNav updated: admin section now has "Progress" (`/admin`) and "Modules" (`/admin/modules`) links. Schema: `CreateModuleSchema` + `CreateMaterialSchema` — `.default()` removed (caused RHF/zodResolver type mismatch; all callers provide explicit values). `npm run build` green. **DoD pending manual test with seeded users.**
- **09 — Group Flow (v1).** `lib/schemas/group.ts` extended with `CreateGroupFormSchema` + `EditGroupFormSchema`; `isGroupFormationOpen()` added to `lib/domain/cutoffs.ts`; `lib/db/users.ts` extended with `getAllLearners()`; `lib/db/groups.ts` extended with `getGroupForUser`, `getOpenGroupsForJoin`, `getAdminGroupsWithEngagement`, `serviceDeleteGroup`; `lib/db/groupMembers.ts` extended with `serviceCreateGroupWithMembers`, `serviceJoinGroup`, `serviceLeaveGroup`, `serviceSetPic`, `getLearnersNotInAnyGroup`; `app/(learner)/group/actions.ts` server actions (createGroup, joinGroup, editGroup, setPic, leaveGroup — each guarded by `isGroupFormationOpen()`); learner components: `GroupCard` (real impl replacing U5 placeholder), `GroupReadView`, `GroupEditForm` (create + edit modes), `MemberSelect`, `PicToggle`; admin component: `GroupAdminTable` (7-day engagement column); pages: `/group`, `/group/create`, `/group/edit`, `/admin/groups`; dashboard page fetches group data in parallel; SidebarNav admin section updated with Groups link; `npm run build` green — 18 routes. **Note:** Join model simplified to open join (any unassigned learner can join any group with < 5 members). **DoD pending manual test with seeded users.**
- **09 — Group Flow (revised spec).** Spec revised per product feedback. Changes: explicit participation mode selection (solo / group / undecided); `db/migrations/0017_participation_mode.sql` adds nullable `participation_mode` column to `users`; `lib/schemas/user.ts` + `ParticipationMode` type; `lib/db/participationMode.ts` (`getParticipationModeField`, `setParticipationMode`); `lib/db/groupMembers.ts` extended with `servicePicAddMember` + `servicePicRemoveMember` (PIC-only member management); `lib/db/groups.ts` extended with `AdminParticipantRow` type, `getAdminAllParticipants` (groups + ungrouped learners as individual rows), `getGroupsWithMemberNames` (for search). New server actions: `chooseIndividual`, `switchToSolo`, `addGroupMemberAction`, `removeGroupMemberAction`. `MemberSelect` now has a name-search text input. `GroupEditForm` edit mode shows PIC-only member management section (add from searchable list, remove with button). New `GroupSearchBar` component — client-side filter by group name or member name. `GroupCard` handles undecided / individual / group states. `/group` page rewritten with mode-selection CTAs + group search. Edit page passes `isPic` + `availableLearners`. `GroupAdminTable` columns changed to Members (names) / Size / Group name (empty for individuals) / Project idea. Admin groups page uses `getAdminAllParticipants`. Dashboard fetches `participationModeField` and passes effective mode to `GroupCard`. `npm run build` green — 29 routes. **DoD pending: run migration 0017, then manual test.**
- **11 — In-Platform Exercises (V1 + post-V1 architecture revision).** 7 migrations total (`0007` → `0013`): `0007_exercises`, `0008_exercise_questions`, `0009_exercise_submissions`, `0010_exercise_answers`, `0011_exercise_uploads_bucket`, `0012_question_types`, `0013_question_options`. **Architecture:** exercise `type` field removed — question type now lives at the question level (`question_type ENUM('free_text','single_choice','multi_choice','file_upload','dropdown')`). Each question also carries `is_optional` and conditional logic fields (`conditional_on_question_id`, `conditional_on_value`). Choice questions (single/multi/dropdown) have options stored in the new `exercise_question_options` table. Zod schemas: `exercise.ts` (no type), `exerciseQuestion.ts` (question_type + is_optional + conditional), `exerciseSubmission.ts`, `exerciseAnswer.ts`, `exerciseQuestionOption.ts` (new). DB access: `lib/db/exercises.ts` (options embedded in question selects via Supabase join), `lib/db/exerciseSubmissions.ts`. Domain: `lib/domain/unlock.ts` + `completion.ts` + `dashboardState.ts` extended; 21 unlock tests green. Admin form: `ExerciseForm` + `QuestionFieldArray` (per-question type selector, required/optional toggle, options editor, conditional logic config, ref answer). Admin pages: `ExerciseListTable`, `SubmissionReviewQueue`, `SubmissionReviewForm`; pages under `/admin/modules/[id]/exercises/`, `/admin/exercises/`, `/admin/exercises/[submissionId]/`. Admin materials page gains Exercises section + "Add exercise" shortcut. Learner: unified `ExerciseForm` (renders correct input per question type, real-time conditional visibility, inline per-question file upload); `ExerciseList`, `ExerciseRow`, `SubmissionStatusBadge`; page + actions at `app/(learner)/exercises/[id]/`. File uploads handled directly in `uploadQuestionFile` server action (no separate API route). Existing: module page has Exercises section + exercise-aware progress bar; reports/heatmap/CSV export extended; SidebarNav Exercises link. `docs/decisions.md` ADR-006 + ADR-007. `npm run typecheck` + `npm run test` + `npm run build` green. **DoD pending: run migrations 0012–0013 against live Supabase, then manual test.**
- **07 — Admin Completion View.** `app/(admin)/layout.tsx` — admin sidebar shell (reuses `SidebarShell`). `lib/db/reports.ts` — `getCohortCompletionMatrix()` (parallel fetch: learner users, modules, materials, all clicks → `Map<userId, Map<materialId, clicked_at>>`); `getUserCompletion(userId)` for drill-in. `lib/schemas/exportFilters.ts` — `TrackFilterSchema`. Components: `CompletionHeatmap` (client, uses `useSearchParams` for client-side track filter; sticky header + first column; links each row to `/admin/users/[id]`), `CompletionCell` (CSS var colors: done=`--state-success`, in-progress=`--accent-primary`, not-started=`--bg-raised`; NA for tech user × non_tech_only module), `TrackFilterChips` (client, `<Link replace>` chips for All/Tech/Non-tech), `ExportButton` (client, `<a download>` to `/api/admin/export-completion`), `UserDrillIn` (server, per-week/module material list with click timestamps). `app/(admin)/admin/users/[id]/page.tsx` — drill-in page, 404 for unknown users. `app/api/admin/export-completion/route.ts` — GET; checks admin role; builds CSV in-memory (no library); streams with `Content-Disposition: attachment`. `npm run build` green. **DoD pending manual test with seeded users.**

- **12 — Logout.** `signOut({ scope: 'local' })` in route handler; `ProfileMenu` sign-out replaced with `useTransition` + `fetch` + `useRouter` (loading spinner, Sonner error toast, double-submit prevention); orphaned `sign-out-button.tsx` deleted. `npm run typecheck` green. **DoD pending manual test.**

- **13 — Module Quizzes.** 2 migrations (`0014_quizzes`, `0015_quiz_attempts`): `quizzes`, `quiz_questions`, `quiz_question_options`, `quiz_attempts`, `quiz_attempt_answers` tables with RLS. Schema: `lib/schemas/quiz.ts`. DB layer: `lib/db/quizzes.ts` (learner reads strip `is_correct`; `submitQuizAttempt` grades server-side in one shot; admin CRUD). Domain: `unlock.ts` + `completion.ts` + `dashboardState.ts` extended with `quizzes` + `passingQuizIds` params (backwards-compatible). 5 new quiz unlock tests (26 total, all green). Admin UI: `QuizForm` (MCQ questions, radio-to-mark-correct, optional explanation per question), `QuizPanel`, `/admin/modules/[id]/quiz/` page + actions; `GraduationCap` icon link added to `ModuleRow`. Learner UI: `QuizCard` (module page status card with Start/Retake links), `QuizTakeForm` (radio form → inline result screen showing per-question breakdown with correct answers + explanations), `/quiz/[id]/` page (shows previous result on load; `?retake=1` forces fresh form). Module page updated: fetches quiz + latest attempt, includes compulsory quiz in progress bar. `npm run typecheck` + `npm run test` (26/26) green. Feature spec: `context/feature-specs/13-quiz.md`. **DoD pending: run migrations 0014–0015 against live Supabase, then manual test.**

- **14 — Admin User Management.** New `/admin/users` route (searchable table with last-activity, role/track badges). Three overflow-menu actions: Assign Track (submenu, optimistic update), Promote to Admin (confirmation dialog, irreversible), Clear History (email-challenge confirmation, hard-deletes clicks + exercise submissions + quiz attempts). Three API route handlers under `/api/admin/users/[id]/track|promote|history` — all server-side role-checked. `getAllUsersWithActivity()` in `lib/db/users.ts` computes last_activity via parallel queries across three tables. Users sidebar link added between Progress and Curriculum. `npm run typecheck` + `npm run build` green. **DoD pending manual test against live Supabase.**

## In Progress

- None.

## Next Up

- Run migrations **0012–0015** (exercises + quizzes) against live Supabase if not yet applied.
- Manual test U13 + U14.
- Deploy to Vercel; set up CI branch protection (deferred from U2).

## Open Questions

- **Hard-gate progression in V1?** The unlock rule is "all compulsory clicked → next module unlocks". Confirm there's no override for admins to advance a learner manually. (Default: no override in V1.)
- **Track assignment source.** Track (`tech` vs `non_tech`) is currently learner-self-selected at first login. Should it be seeded from an HR CSV instead, to prevent miscategorization? (Default: self-select for V1.)
- **Group submission cutoff.** End of Week 5 is approximate. Need a specific date and time, hardcoded in `/lib/domain/cutoffs.ts`.
- **PIC selection.** Within a group, how is the PIC chosen — first to submit, or explicit `is_pic` toggle? (Default: explicit toggle, exactly one PIC per group, enforced in DB.)
- **Already-completed material click behavior.** Re-clicking a completed material — does it update `clicked_at` or stay first-click-wins? (Default per architecture invariants: first click wins, never updated.)
- **Pelago brand colors.** Current palette is the warm-but-clean default. Confirm whether to override with Pelago brand colors when we receive the brand sheet.
- **CSV export columns.** Decide on the exact column set for the admin CSV export. (Default candidate: `email, name, track, module_title, material_title, is_compulsory, clicked_at`.)

## Architecture Decisions

- **Stack: Next.js + Supabase + Vercel.** Best AI-coding-assistant ecosystem for a solo build; auth + Postgres + storage in one platform; free tier covers Cohort 1.
- **Content lives outside the platform.** Materials are external links (Google Docs, Slides, YouTube, Forms). The DB stores metadata only. Keeps authoring in Google Workspace and the DB tiny.
- **Click = completion.** No "Mark complete" button. Clicking a material link records completion server-side and 302-redirects to the external URL. Single code path, idempotent via `UNIQUE (user_id, material_id)`.
- **Compulsory vs optional materials.** A module unlocks the next when all of its compulsory materials are clicked. Optionals never gate.
- **Three admins, allowlist-only.** Megan, Data Lead, Boss. Seeded by email; no admin promotion UI. HR is not an admin.
- **Two roles only.** `learner`, `admin`. No reviewer role in V1 (capstone review is off-platform).
- **Single hardcoded cohort.** No multi-cohort support. Dates live in `/lib/domain/cutoffs.ts`.
- **RLS + middleware = two layers of authz.** Application-level guards first, RLS policies second. Both must be in place for any non-public table.
- **`/lib/db/*` is the only DB access layer.** Pages and routes never call Supabase directly.
- **Migrations are append-only.** No editing merged migrations.
- **No file storage in V1.** Capstone artifacts live in Google Drive, referenced by URL.
- **No AI in the product, no background jobs.** All work is synchronous request/response. CSV export builds in-memory.
- **Light only, warm-but-clean palette.** Off-white background, soft teal accent. Friendly without being playful.

## Session Notes

- Workspace: `/code/training-platform/` under the P1-AI-Training project.
- All six context files now live in `/code/training-platform/context/`.
- Timeline: today is 2026-05-15. Cohort kickoff is June 2026 — under 4 weeks to ship V1.
- Builder: solo (Megan), AI-assisted (Claude Code / Cursor).
- Build order is locked: U1 → U2 → U3 → U4 → U5 → U6 → U7 → U8 → U9, one half-week per unit on average.
- Next action: open a new chat focused on **U1 — Foundations**, with the five spec files as context.
